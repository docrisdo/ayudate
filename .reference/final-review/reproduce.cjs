const {chromium}=require('playwright');(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files/BraveSoftware/Brave-Browser/Application/brave.exe',headless:true});const p=await b.newPage();const click=async n=>p.getByRole('button',{name:n,exact:true}).filter({visible:true}).first().click();await p.goto('http://localhost:5173/');await click('Continuar');console.log('Contrast',await p.locator('.ac-bottom-actions .lists-button--blue,.ac-hero h1 span').evaluateAll(es=>es.map(e=>({text:e.textContent,color:getComputedStyle(e).color,background:getComputedStyle(e).backgroundColor}))));await click('Guardar y continuar');await click('Mis listas');await click('Nueva lista');await p.getByLabel('Nombre de la lista',{exact:true}).fill('Cantidades');await p.getByLabel('Escribe tus productos (uno por línea)').fill('2 x Leche');await click('Agregar productos');await click('Guardar e iniciar compra');await click('Producto encontrado');await click('Mi carrito');await p.getByRole('button',{name:'Quitar una unidad de Leche deslactosada',exact:true}).click();await click('Mi ruta');console.log('Route',await p.locator('.rt-progress').innerText());await click('Inicio');console.log('Home',await p.locator('.home-purchase-progress').innerText());await b.close()})()
