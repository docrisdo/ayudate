const {chromium}=require('playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files/BraveSoftware/Brave-Browser/Application/brave.exe',headless:true});const p=await b.newPage();await p.goto('http://localhost:5173/');await p.getByRole('button',{name:'Continuar',exact:true}).click();await p.getByRole('button',{name:'Guardar y continuar',exact:true}).click();await p.getByRole('button',{name:'Solicitar asistencia',exact:true}).filter({visible:true}).first().click();assert.equal(await p.locator('.as-actions .lists-button img').evaluate(e=>getComputedStyle(e).backgroundColor),'rgb(255, 255, 255)');assert.equal(await p.locator('.as-notify .lists-button img').evaluate(e=>getComputedStyle(e).backgroundColor),'rgba(0, 0, 0, 0)');console.log('Reading and assistance icons retain contrasting backgrounds');await b.close()})()
